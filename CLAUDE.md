# 이 저장소에서 글을 쓰기 전에 — 유료 상품과 겹치면 안 된다

이 블로그는 **무료로 공개**된다. 그리고 같은 소재로 만든 **유료 전자책**이 따로 팔리고 있다.
여기에 책의 결론을 적으면 파는 물건을 공짜로 주는 셈이 된다.

> **판매 중**: 「클로드 코드가 못 한다고 할 때, 실제로는 되는 8가지 (PDF 20쪽)」
> https://latpeed.com/products/C1qGw · 4,900원 · A4 20쪽 · 래피드(Latpeed)
> 원고 원본: `liquidation-man/side-projects` → `monetization/ebook-claude-orca/manuscript.md`
> 리스팅 정본: `liquidation-man/biz` → `channels/ebook/README.md`
>
> ⚠️ **상품명은 판매 페이지가 최종 권위다.** 2026-08-08 에 래피드에서 이름을 바꿨는데
> 이 문서와 글 하단 CTA 가 옛 이름으로 남아 **없는 상품을 광고하고 있었다.**
> 이름을 고치는 날은 여기와 `layouts/_partials/extend_post_content.html` 을 같이 고친다.

## 쓰면 안 되는 것 — 이게 이 문서의 핵심이다

**클로드 코드(또는 유사 AI 코딩 도구) 자체의 동작·오류·함정과 그 원인**을 글로 쓰지 않는다.
개발일지 자동 발행에도 그대로 적용되고, 2026-08-07부터는 **발행 전에 기계가 막는다**
(아래 「자동 검사가 걸린다」 ②).

구체적으로 아래 여덟 가지는 **책의 각 장 주제**다. 증상만 언급하는 것도 피하고,
**원인이나 해결책은 절대 쓰지 않는다.**

| # | 주제 | 쓰면 안 되는 결론의 예 |
|---|---|---|
| 1 | 인증이 막혔다며 사용자에게 넘기는 것 | 「자격증명은 처음부터 있었다」 |
| 2 | 승인 프롬프트가 창마다 다른 것 | 「통합 도구는 무승인 인자를 기본으로 붙인다」 |
| 3 | 파일·링크·이미지 접근 범위 | 「커넥터는 계정에, 폴더 도구는 폴더에 붙는다」 |
| 4 | 토큰·컨텍스트 한도 | 「한도가 5시간 창과 주간으로 두 겹이다」 |
| 5 | 「완료했습니다」 후 번복 | 「확인 안 한 것을 확인한 것처럼 말한다」 |
| 6 | 「설치가 안 돼 있다」 오진 | **「축소된 PATH 때문이다」** ← 가장 자주 새어 나갈 문장 |
| 7 | 「직접 하셔야 합니다」 떠넘김 | 「명령 한 줄로 되는 경우가 많다」 |
| 8 | 「예」를 누르기 전 확인 목록 | 되묻는 말 다섯 개 |

### 예외 하나 — 사람이 쓰는 홍보글은 「맛보기」까지 허용한다 (2026-08-08 사용자 결정)

위 표는 **자동 발행에 대해서는 그대로 전면 금지**다. 개발일지는 매일 밤 사람 없이
나가고, 판정하는 LLM 이 「어디까지가 맛보기인가」를 매번 혼자 정하게 되면 그 경계는
언젠가 넘어간다. **자동 경로에서는 감도 조절을 하지 않는다.**

**사람이 직접 쓰는 홍보글은 다르다.** 책을 팔려면 책 이야기를 해야 한다. 다만
**파는 것을 공짜로 주지 않는 선**을 지킨다:

| | 써도 된다 | 쓰면 안 된다 |
|---|---|---|
| **증상·현상** | ✅ 「도구가 없다고 한 것이 사실은 있었다」 | |
| **목차·사례 수** | ✅ 「이런 사례 8가지를 모았다」 | |
| **원인** | | ❌ 「원인은 ○○다」 |
| **해결책·명령어** | | ❌ 재현 가능한 절차 |
| **본문 인용** | ✅ 한두 문장, 출처 표기 | ❌ 한 절 통째로 |

기준 한 줄: **읽고 나서 「그래서 어떻게 고치지?」가 남아야 맛보기다.**
읽은 사람이 그것만으로 문제를 해결할 수 있으면 그건 맛보기가 아니라 책의 대체재다.

> ⚠️ **이 예외는 자동 검사를 풀지 않는다.** `.github/ebook-protected-phrases.txt` 는
> 그대로 두고, 사람이 쓴 홍보글이 검사에 걸리면 **글을 고치는 쪽을 먼저 본다.**
> 문구를 목록에서 빼는 것은 그 문구가 정말 책의 결론이 아닐 때만이다.

## 그럼 뭘 쓰나

**무엇을 만들었는지**는 얼마든지 쓴다. 그게 개발일지의 원래 목적이다.
막는 것은 **도구 자체의 함정과 그 해법**뿐이다.

- ✅ 「전자책 판매 페이지를 만들고 결제를 붙였다」
- ✅ 「Hugo 파셜로 글 하단 CTA를 자동화했다」
- ❌ 「클로드가 없다고 한 프로그램이 사실은 PATH 문제였다」

## 자동 검사가 걸린다 — **층이 둘이다**

**① 발행 후 (이 저장소).** `.github/workflows/no-ebook-spoiler.yml` 이 새 글에서 위 주제의
특징적인 문구를 찾는다. 걸리면 **빨간 X**가 뜬다. **배포는 막지 않는다** — 이 블로그는
한 번 여섯 달 죽은 적이 있어서 검사 실패로 사이트가 내려가는 구조는 만들지 않았다.

빨간 X가 뜨면 사람이 판단해서 글을 고치거나 revert 한다.
검사에 걸렸는데 문제없는 글이면 `.github/ebook-protected-phrases.txt` 에서 그 문구를 뺀다.

**② 발행 전 (개발일지 자동 발행 파이프라인).** 2026-08-07 추가. `biz` 저장소의
`channels/blog/devlog/check_ebook.py` 가 **push 하기 전에** 같은 목록으로 한 번 더 본다.

①만으로는 자동 발행을 못 막는다. 개발일지는 매일 23:30 에 사람 없이 나가고, ①은 그
뒤에 도는 데다 배포를 막지 않으므로, 새어 나간 문장은 **누군가 빨간 X 를 볼 때까지
공개 웹에 그대로 있다.** ②에 걸리면 그 원고를 버리고 통계 템플릿으로 대체한다 —
글은 나가고 결론은 안 나간다. 커밋 제목도 같은 검사를 받는다(템플릿이 제목을 그대로
싣기 때문이다).

> 🔑 **문구 목록의 정본은 이 저장소다.** ②는 목록을 복사해 두지 않고 클론한 이 저장소의
> `.github/ebook-protected-phrases.txt` 를 직접 읽는다. **고칠 곳은 여기 한 곳이고,
> `biz` 쪽은 손댈 필요가 없다.**
>
> 사람이 직접 쓰는 글은 ②를 지나지 않으므로 ①은 그대로 필요하다. 둘은 서로를 대신하지 않는다.

---

## 그 밖에 이 저장소에서 알아야 할 것

- **테마는 서브모듈**이다(PaperMod, `154d006e`). 처음 클론하면 비어 있으니
  `git submodule update --init --depth 1 themes/PaperMod`.
- **레이아웃 확장 지점은 `layouts/_partials/`** 다(새 Hugo 구조).
  `extend_head.html` · `extend_footer.html` · `extend_post_content.html` 셋이고,
  글 본문 아래에 뭘 붙이려면 **`extend_post_content.html`** 이다.
  옛 경로 `layouts/partials/` 도 아직 먹지만, 헷갈리니 새 경로를 쓴다.
- ⚠️ **「이 PC 에는 Hugo 가 없다」는 이제 틀렸다 (2026-08-08).** 설치는 안 돼 있지만
  **포터블 바이너리를 받으면 로컬 빌드 검증이 된다.** 실제로 그렇게 검증했다:

  ```bash
  curl -sSL -o hugo.zip https://github.com/gohugoio/hugo/releases/download/v0.164.0/hugo_extended_0.164.0_windows-amd64.zip
  unzip -q hugo.zip && ./hugo.exe --gc --minify --destination <임시폴더>
  ```

  버전은 `.github/workflows/hugo.yml` 의 `HUGO_VERSION` 과 맞춘다(지금 `0.164.0`).
  **템플릿을 건드렸으면 push 전에 한 번 돌려라** — 「복잡하게 쓰지 마라」로 피할
  일이 아니었다. 스크래치패드에 받아서 쓰고 저장소에는 커밋하지 않는다.
  (파이프로 넘기면 종료 시 goroutine 덤프가 찍히는데 **빌드는 성공이다** —
  `exit=0` 과 `Total in …` 을 보라. 깨진 것으로 오해하지 말 것.)
- 슬러그가 파일명과 다를 수 있다 (예: `2026-07-31-개발일지.md` → `/posts/devlog-2026-07-31/`).
- **카테고리 = 프로젝트다 (2026-08-08 사용자 결정).** 기술 분야(「자동화」·「생성형 AI」)로
  나누던 것을 프로젝트명(「라이더 로그」·「지옥철」·「트레이딩 봇」)으로 바꿨다.
  `/projects/` 페이지(`layouts/projects.html`)가 이 축을 최근 활동순으로 세운다.
  - **글 제목도 `프로젝트명 — 한 줄` 형식으로 통일한다.** 20자 안팎.
  - **새 카테고리를 즉흥으로 만들지 마라.** 프로젝트 정본 목록은 `CONTEXT.md` §1
    「프로젝트 지도」다. 거기 없는 이름을 쓰면 `/projects/` 에 유령 항목이 생긴다.
  - 자동 개발일지는 그날 움직인 프로젝트를 카테고리로 단다
    (`biz` → `channels/blog/devlog/generate_draft.py`). **이름이 여기 카테고리와
    글자 단위로 같아야 한 칸에 모인다.**
  - **파일명은 건드리지 않았다** — 파일명이 곧 URL 이라 바꾸면 기존 링크가 깨진다.
    제목만 바꿨으므로 주소는 그대로다.
- 전체 맥락은 `liquidation-man/side-projects` 의 `CONTEXT.md` 에 있다.
