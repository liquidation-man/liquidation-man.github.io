# Hugo 빌드 산출물 (GitHub Actions가 서버에서 빌드하므로 커밋하지 않음)
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json

# OS
.DS_Store
Thumbs.db

# 크레덴셜 — `.env*` 로는 부족하다. `.env.bak`(뒤에 붙는 것)도
# `.en`(잘린 것)도 그 패턴을 빠져나간다. 2026-07-30·08-10 에 둘 다 겪었다.
# `.e*` 까지 넓히면 `.eslintrc`·`.editorconfig` 를 잡아먹는다.
.en*
!.env.example
